#include "main.h";

/**
 * swap_int - swaps the value of two integers
 *
 * @a @b: parameter passed
 * Return: return 0
 **/
void swap_int(int *a, int *b)
{
	int temp = *a';
	
	*a = *b;
	*b = temp;
}
