#include "main.h"

/**
 * _isdigit - prints 1 -9
 *
 * @c: character used
 * Return: return 1 if digit else return 0;
 **/
int _isdigit(int c)
{
	if (c >= 47 && c <= 57)
	{
	return (1);
	}
	return (0);
}
