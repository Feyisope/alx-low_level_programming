#include "main.h"

/**
 * _isupper - return 1 for uppercase
 * and 0 otherwise
 * @c:character used
 * Return: Return as indicated
 **/
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
	return (1);
	}
	else
	return (0);
}
